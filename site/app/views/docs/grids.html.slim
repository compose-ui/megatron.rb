- main_header
  h2 Grids

- main
  /- tabs do |t|
    /= t.tab 'Grids', '/grids'
    /= t.tab 'Boxes', '/boxes'

  .grid-demo
    = heading "Using Grids"
    code: slim
      - grid [options] do |g|       # You'll use the grid block
        = g.cell [size], [options]  # and grid cells
          p Content!
    
    = heading "Grid/Cell Options"

    p Grid blocks can accept these options (defaults are listed first).

    code: ruby
      gutters: 'medium', 'none', 'small', 'large'
      gutter_matching: false, true
      align:   'left', 'center', 'right'
      valign:  'top', 'middle', 'bottom'
      
    p Cells accept an optional size followed by a hash of options.

    code: ruby
      g.cell [1,2], [options]

      # responsive size options
      xsmall: []
      small:  []
      medium: []
      large:  []
      xlarge: []

    p Here's an example:

    = demo_box 'Simple grid'
      - grid do |g|
        = g.cell
          code grid-cell
        = g.cell [1, 3]
          code=width(1,3)
        = g.cell [1, 3]
          code=width(1,3)
        = g.cell [1, 3]
          code=width(1,3)

    = render "docs/grid/cell_sizes"
    = render "docs/grid/responsive_cell_sizes"
    = render "docs/grid/gutter_sizes"
    = render "docs/grid/gutter_matching"
    = render "docs/grid/nested_grids"
    = render "docs/grid/horizontal_alignment"
    = render "docs/grid/vertical_alignment"
